import { useEffect, useState } from "react";
import axios from "axios";
import { Link } from "react-router-dom";

export default function FavorisPage() {
  const [isLoading, setIsLoading] = useState(true);

  return <div>favoris page</div>;
}
